<div class="field">
    <label>{{name}}</label>
    <div>
        {{#options}}
            <input type="radio" id="{{id}}" name="{{name}}" value="{{value}}">
            <label for="{{id}}">{{name}}</label>
            <br/>
        {{/options}}
    </div>
</div>